/**
 * Shared public types for Stage. Keep this surface small until the preview graph stabilizes.
 */

export type StageVersion = string;

export type PreviewTarget = {
  /** Human-readable label for logs and UI. */
  label: string;
};
